const CREDIT_CARD = 'credit-card';
const CREDIT_CARD_NAME = `${CREDIT_CARD}-name`;
const CREDIT_CARD_AMOUNT = `${CREDIT_CARD}-amount`;
const CREDIT_CARD_TITLE = `${CREDIT_CARD}-title`;
const CREDIT_CARD_FOOTER = `${CREDIT_CARD}-footer`;

module.exports = {
  CREDIT_CARD,
  CREDIT_CARD_NAME,
  CREDIT_CARD_AMOUNT,
  CREDIT_CARD_TITLE,
  CREDIT_CARD_FOOTER,
};
